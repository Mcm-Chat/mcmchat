<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.mcm.privateconnect.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBarLaunch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links (diverifikasi lewat /.well-known/assetlinks.json) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/chat" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/call" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/status" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/tasks" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/prepare" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/ledger" />
                <data android:scheme="https" android:host="mcmchat.id" android:pathPrefix="/delete-account" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/chat" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/call" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/status" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/tasks" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/prepare" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/ledger" />
                <data android:scheme="https" android:host="www.mcmchat.id" android:pathPrefix="/delete-account" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Kanal notifikasi default MCM -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="mcm_chat" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_mcm" />
    </application>

    <!-- Izin minimum. Tidak ada background location, tidak ada MANAGE_EXTERNAL_STORAGE. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
